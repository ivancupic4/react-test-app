A simple hello world React app, trying out lists, buttons, alerts. 

Start via terminal by navigating to project folder and running `npm run dev`. Vite will start the app on `http://localhost:5173/`.
